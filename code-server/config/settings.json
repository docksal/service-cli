{
  // XDebug Launch Configuration settings
  "launch": {
    "version": "0.2.0",
    "configurations": [
      {
        "name": "XDebug: web",
        "type": "php",
        "request": "launch",
        "port": 9001
      },
      {
        "name": "XDebug: cli (currently open script)",
        "type": "php",
        "request": "launch",
        "program": "${file}",
        "cwd": "${fileDirname}",
        "port": 9001
      }
    ]
  }
}
